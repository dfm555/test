import {{lowerCase name}}Reducer from './{{lowerCase name}}'

describe('{{pascalCase name}} Reducer', () => {
	it('Should return default state', () => {
		const newState = {{lowerCase name}}Reducer(undefined, {})
		expect(newState).toEqual({ loading: false })
	})
})
